# API base URL (no trailing slash). Used when frontend is served via ngrok so it can reach the backend.
# Leave unset for local dev (defaults to http://localhost:8000).
# VITE_API_URL=https://your-backend-subdomain.ngrok-free.app
